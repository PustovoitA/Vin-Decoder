import styles from "./From.module.css"

const Form = () => {
    return(<>
        <form>
            <input type="text" />
            <button type="submit">Decode</button>
        </form>
    </>)
}

export default Form